{
    "name": "cypress-example",
    "version": "1.0.0",
    "engines": {
        "npm": ">=8.0.0",
        "node": ">=18.0.0"
    },
    "scripts": {
        "lint": "npx eslint . --fix",
        "test": "npx cypress run --browser chrome"
    },
    "devDependencies": {
        "cypress": "^10.11.0",
        "eslint": "^8.26.0",
        "eslint-plugin-cypress": "^2.12.1"
    }
}
