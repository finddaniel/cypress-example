{
    "name": "cypress-example",
    "version": "1.0.0",
    "engines": {
        "npm": ">=8.0.0",
        "node": ">=18.0.0"
    },
    "scripts": {
        "lint": "npx eslint . --fix",
        "test": "npx cypress run --browser chrome"
    },
    "devDependencies": {
        "cypress": "^10.10.0",
        "eslint": "^8.25.0",
        "eslint-plugin-cypress": "^2.12.1"
    }
}
